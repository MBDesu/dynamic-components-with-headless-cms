import { Routes } from '@angular/router';
import { ContentfulContent } from './components/contentful-content/contentful-content';

export const routes: Routes = [
  { path: '', component: ContentfulContent }
];
