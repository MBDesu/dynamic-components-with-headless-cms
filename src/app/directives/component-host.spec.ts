import { ComponentHost } from './component-host';

describe('ComponentHost', () => {
  it('should create an instance', () => {
    const directive = new ComponentHost();
    expect(directive).toBeTruthy();
  });
});
